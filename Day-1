# 1️⃣ Navigate to your repository
cd /path/to/your/repository

# 2️⃣ Create and switch to the "day1-task" branch
git checkout -b day1-task  # If branch doesn't exist
# OR switch to the branch if it already exists
git checkout day1-task  

# 3️⃣ Move or copy your document to the repo (modify path as needed)
mv ~/Downloads/day1-task.docx .  # If it's a Word file
# OR
cp ~/Downloads/day1-task.md .     # If it's a Markdown file

# 4️⃣ Stage the document for commit
git add day1-task.docx   # If using .docx
# OR
git add day1-task.md     # If using .md

# 5️⃣ Commit the changes with a message
git commit -m "Added Day 1 task document"

# 6️⃣ Push the branch to GitHub
git push origin day1-task

# 7️⃣ Verify the document on GitHub (Manual Step)

